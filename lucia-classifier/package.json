{
  "name": "lucia-classifier-deploy",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build && cd .. && rm -rf dist && mv frontend/dist ./dist"
  }
}
