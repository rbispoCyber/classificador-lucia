{
  "name": "lucia-classifier-deploy",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "electron": "npm run electron --workspace=frontend"
  }
}
